(function() { 'use strict'; const CONFIG = { domains: ['juliolp.pt', 'www.juliolp.pt'], gracePeriod: 3000, checkInterval: 5000, showMessage: true }; function isDomainAuthorized() { const hostname = window.location.hostname; return CONFIG.domains.some(domain => hostname === domain || hostname.endsWith('.' + domain) ); } function disableFeatures() { const criticalIds = [ 'cartSidebar', 'cartOverlay', 'addToCartBtn', 'js-products-grid', 'js-pagination', 'wishlist-grid' ]; criticalIds.forEach(id => { const el = document.getElementById(id); if (el) el.remove(); }); document.querySelectorAll('.btn-add-cart, .btn-wishlist, .btn-checkout').forEach(btn => { btn.remove(); }); try { localStorage.clear(); sessionStorage.clear(); } catch(e) {} if (window.simpleCart) { try { window.simpleCart.empty(); } catch(e) {} } if (CONFIG.showMessage) { document.body.innerHTML = ` <div style=" display: flex; flex-direction: column; justify-content: center; align-items: center; min-height: 100vh; padding: 20px; font-family: 'Poppins', Arial, sans-serif; background: linear-gradient(135deg, #1a1a1a 0%, #2d2d2d 100%); color: #fff; text-align: center; "> <div style=" max-width: 500px; background: rgba(255,255,255,0.05); padding: 40px; border-radius: 20px; backdrop-filter: blur(10px); box-shadow: 0 8px 32px rgba(0,0,0,0.3); "> <h1 style=" color: #d4af37; font-size: clamp(24px, 5vw, 36px); margin-bottom: 20px; font-weight: 700; ">🔒 Domínio Não Autorizado</h1> <p style=" color: rgba(255,255,255,0.8); font-size: clamp(14px, 3vw, 16px); line-height: 1.6; margin-bottom: 30px; "> Este template foi desenvolvido com dedicação e está protegido por direitos autorais. </p> <div style=" background: rgba(212,175,55,0.1); padding: 20px; border-radius: 10px; border: 1px solid rgba(212,175,55,0.3); "> <p style=" color: #d4af37; font-size: clamp(12px, 2.5vw, 14px); margin: 0; "> <strong>Licenciado exclusivamente para:</strong><br> juliolp.pt </p> </div> <p style=" color: rgba(255,255,255,0.5); font-size: clamp(11px, 2vw, 12px); margin-top: 30px; "> © ${new Date().getFullYear()} JulioLP - Todos os direitos reservados </p> </div> </div> `; } } function initialCheck() { if (!isDomainAuthorized()) { console.warn('⚠️ Domínio não autorizado detectado'); setTimeout(() => { if (!isDomainAuthorized()) { disableFeatures(); } }, CONFIG.gracePeriod); } } function continuousCheck() { setInterval(() => { if (!isDomainAuthorized()) { disableFeatures(); } }, CONFIG.checkInterval); } Object.freeze(CONFIG); Object.defineProperty(window, 'julioDomainCheck', { value: isDomainAuthorized, writable: false, configurable: false }); if (document.readyState === 'loading') { document.addEventListener('DOMContentLoaded', () => { initialCheck(); continuousCheck(); }); } else { initialCheck(); continuousCheck(); } })();
